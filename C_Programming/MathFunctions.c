#include <stdio.h>
#include <math.h>
int main(){
    float a=3.4;

    //a=sqrt(a);
    //a=pow(a,3);
    //a=round(a);
    //a=ceil(a);
    //a=floor(a);
    //a=abs(a); //how far it is from zero is the alsolute value
    //a=log(a);
    //a=sin(a);
    //a=cos(a);
    //a=tan(a);

    printf("%f",a);
    return 0;
}